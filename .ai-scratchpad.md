# AI SCRATCHPAD

Status: IMPLEMENTATION_READY

Claude writes here.

Codex implements from here only when Status is `IMPLEMENTATION_READY`.

Codex validates from here only when Status is `IMPLEMENTATION_READY` or `VALIDATION_READY`.

Do not store long-term project docs here.

---

## Task Summary

Evolve the Tyvera Assistant from its production navigation-helper behavior into a full operations assistant. Source inspection shows the operations assistant **already largely exists** in `apps/api/src/help/` (18 read/draft OpenAI tools, 2 confirmed-mutation tools with HMAC confirmation, tool-risk policy, SSE streaming, thread memory) but is **dormant** behind three feature flags that default `false`. Work = enable the native path, harden it (audit logging is missing on confirmed mutations), then extend the registry with new reads, diagnostics, and two new confirmed actions. Deliver in independent phases.

## Task Type
Feature (multi-phase). Phases 3–4 include Deep mutation work.

## Task Size
Deep (Automations, Messaging, Billing/SMS, Auth/Permissions, Transactions all in scope).

## Human Approval
- RCA approved: N/A (feature, not bug)
- Discovery approved: yes (2026-06-20)
- Plan approved: yes (2026-06-20)
- Deep implementation approved: NOT YET — Phases 3–4 (mutations) require separate approval before execution. Phases 0–2 approved.

## Confirmed Facts

Stack correction: persistence is **Drizzle**, not Prisma. Evidence: `apps/api/src/security/audit-log.service.ts:2-3` (`import { getDb } from "@tyvera/database"`); schema `packages/database/src/schema/index.ts`; migrations `packages/database/drizzle/*.sql`. No Prisma schema. Any "migration" = a Drizzle migration.

Assistant lives in `apps/api/src/help/` (HelpModule), NOT `apps/api/src/ai/` (that module is the guardrail/usage substrate).

Routes (verified `assistant.controller.ts`): `POST /help/assistant/chat`, `POST /help/assistant/chat/stream` (SSE), `POST /help/assistant/actions/confirm`. Guard `ClerkAuthGuard`; tenant via `@Tenant("organizationId"|"userId")`; `businessId` from request body (`workspace.activeBusinessId`).

Dormancy cause (verified `apps/api/src/common/feature-flags.service.ts:100-102`): `openai_native_assistant_stream_enabled`, `openai_native_assistant_tools_enabled`, `openai_native_assistant_mutations_enabled` all default `false`. With stream flag off, `assistant.service.ts:249-269` falls back to legacy deterministic `chatStream` (5 fixed intents).

Existing tools (verified):
- 18 read/draft in `assistant-openai-tools.service.ts:10-29` (get_business_summary, get_sms_usage, get_billing_status, get_ai_usage, route_guidance, get_owner_daily_briefing, get_appointments_summary, get_needs_review_summary, get_manual_followup_summary, get_automation_health_summary, get_customer_audience_count, get_duplicate_customer_summary, get_business_performance_comparison, get_booking_availability, get_message_delivery_health, find_customers, list_appointments, draft_winback_message, draft_reminder_message).
- 2 confirmed mutations in `assistant-mutation.service.ts`: `update_customer`, `reschedule_appointment`. HMAC-signed token (`AUTH_SESSION_SECRET`), 5-min TTL, nonce, scope match, `timingSafeEqual`, snapshot stale-check, read-only-account guard (`:291-297`).

Tool-risk policy is centralized in `assistant-tool-policy.ts` (`risk`, `requiresBusinessScope`, `maximumRows`, `piiMode`, `auditRequired`). Every new tool MUST add an entry here.

Execution guardrails (verified `ai-execution.service.ts`): `MAX_ASSISTANT_TOOL_ROUNDS=4`, `MAX_ASSISTANT_TOOL_CALLS=6` (`:13-14`), `allowedToolNames` enforcement, `parallel_tool_calls:false`, model hardcoded `gpt-4o-mini` (`:143,:280`), usage recorded.

Frontend (`apps/web/src/components/tyvera-assistant.tsx`) already renders streaming, confirmation cards (`ConfirmActionInline`), `draft_only` notices, action chips, usage snapshot. Confirmation action union hardcoded to `"update_customer" | "reschedule_appointment"` (`:46-48`).

Reuse targets confirmed: `OnboardingService.getProgress` (`onboarding.service.ts:8`), `AutomationSettingsService.getSnapshot`/`update` (`:87,:98`), `ClientBillingRequestService.create`/`listForOrganization` (`:43,:105`), `BusinessesService.findById` (`:68`, name/businessType only), `AuditLogService.log` (`audit-log.service.ts:22`), `operations/automation-job-run.service.ts` (writes `automationJobRuns`, no read method).

GAP (highest priority): confirmed mutations write NO audit log. `assistant-mutation.service.ts` never imports/calls `AuditLogService`, despite `auditRequired:true` in policy (`assistant-tool-policy.ts:73,80`). `AuditAction` union (`audit-log.service.ts:5-11`) lacks customer/appointment/automation/topup actions.

Not verified in current code: `getServices`/`getBusinessHours` source; public booking-page disabled flag; appointment create/cancel service methods.

## Files To Modify

### File: `apps/api/src/common/feature-flags.service.ts`
- Location: `FLAG_DEFAULTS` (`:94-102`) + env rollout
- Change: staged enablement of `openai_native_assistant_stream_enabled` then `..._tools_enabled` (non-prod first); `..._mutations_enabled` only after audit work (Phase 0) lands
- Reason: activate dormant native path
- Contract impact: none (env-driven)
- Test impact: assert legacy fallback when flags off and when `hasOpenAi()` false

### File: `apps/api/src/help/assistant-openai-tools.service.ts`
- Location: `getToolDefinitions()` + `execute()` switch + `AssistantNativeReadToolName` union
- Change: add Phase 1 read tools (`get_onboarding_status`, `get_subscription_and_limits`, `get_customer_timeline`, `get_appointment_details`, `get_automation_runs`, `get_billing_requests`) and Phase 2 diagnostics (`diagnose_reminder_issue`, `diagnose_booking_issue`, `explain_message_status`, `explain_sms_usage`, `get_recommended_next_actions`)
- Reason: extend read/diagnostic coverage
- Contract impact: new tool defs only (additive)
- Test impact: `assistant-openai-tools.service.spec.ts`

### File: `apps/api/src/help/assistant-tool-policy.ts`
- Location: `ASSISTANT_TOOL_POLICIES`
- Change: one policy entry per new tool (risk/scope/maxRows/piiMode/auditRequired)
- Reason: single guardrail source of truth
- Test impact: `assistant-tool-policy.spec.ts`

### File: `apps/api/src/help/assistant-read-model.service.ts`
- Location: new read methods
- Change: implement new reads + diagnostic compositions, reusing injected domain services; PII-safe outputs (mask, no raw provider errors)
- Test impact: `assistant-read-model.service.spec.ts`

### File: `apps/api/src/help/help.module.ts`
- Location: `imports`
- Change: add modules for new dependencies (e.g. `OnboardingModule`) if a needed service is not already injectable
- Test impact: module compiles; integration

### File: `apps/api/src/operations/automation-job-run.service.ts`
- Location: new method
- Change: add `listRecentRuns(organizationId, businessId, days)` — Drizzle select on `automationJobRuns`
- Reason: backs `get_automation_runs`
- Contract impact: internal service method
- Test impact: new spec

### File: `apps/api/src/security/audit-log.service.ts`
- Location: `AuditAction` union (`:5-11`)
- Change: add `assistant_customer_update`, `assistant_appointment_reschedule`, `assistant_automation_settings_update`, `assistant_sms_topup_request`
- Reason: audit confirmed mutations
- Contract impact: extends audit taxonomy (confirm naming with audit-schema owner)

### File: `apps/api/src/help/assistant-mutation.service.ts`
- Location: constructor + `confirm()` + new `proposeX` branches
- Change: inject `AuditLogService`, call `log()` after successful mutation; enforce `auditRequired`; add `update_automation_settings` + `create_sms_topup_request` proposal/confirm branches reusing `createProposal`/`signPayload`/`verifyToken`
- Reason: close audit gap (Phase 0); add Phase 3/4 actions
- Contract impact: new confirmation actions in SSE `confirmation` event + `/actions/confirm`
- Test impact: `assistant-mutation.service.spec.ts`

### File: `apps/web/src/components/tyvera-assistant.tsx`
- Location: confirmation union (`:46-48,:444-449`), chip renderer (`:845-862`)
- Change: generalize confirmation action union (drive label/summary from server payload); add diagnostic result card + recommended-next-actions list
- Reason: render new actions/diagnostics
- Test impact: `tyvera-assistant.test.tsx`, `tyvera-assistant-orchestration.test.tsx`

### File: route whitelists (BOTH must stay in sync)
- `apps/api/src/help/assistant-openai-tools.service.ts:40-50` (`SAFE_ROUTES`) and `apps/api/src/help/assistant.service.ts:18-28` (`SAFE_ROUTE_WHITELIST`)
- Change: add any new fix/action route or chips silently drop (`assistant.service.ts:980`)

## Exact Changes Per File
See "Files To Modify" + phased order below. New tool defs follow existing conventions: `OpenAI.Responses.Tool`, `strict:true`, `additionalProperties:false`, all props `required` (nullable via `["string","null"]`). New confirmed actions reuse the existing HMAC proposal/confirm machinery verbatim — only add `proposeX`/confirm branches.

## API Contract Changes
Additive only. New SSE `confirmation` actions and new `/actions/confirm` payloads for `update_automation_settings` and `create_sms_topup_request`. Existing `/help/assistant/*` shapes unchanged (matches `docs/ai/contracts/api-contracts.md` row 37). No route additions.

## Database / Schema Changes
Extend `AuditAction` enum in code; verify whether `auditLogs.action` is a DB enum or free text in `packages/database/src/schema/index.ts` before deciding if a Drizzle migration is required. No other schema changes. State after verification.

## Contract Areas
- API: `/help/assistant/chat`, `/chat/stream`, `/actions/confirm` (additive); reads from automation settings (row 29) and client billing requests (row 26)
- Database: `auditLogs` (action taxonomy); read-only selects on `automationJobRuns`
- Permissions: Clerk + `@Tenant` org/business scope; read-only-account guard for mutations
- External integrations: OpenAI (gpt-4o-mini) via existing execution pipeline
- Jobs / automations: read `automationJobRuns`; `AutomationSettingsService.update` for the new confirmed action

## Risk Register Notes
- UNMAPPED RISK: confirmed-mutation audit logging absent today — must be closed in Phase 0 before the mutations flag is enabled.
- Deep defaults apply: Automations, SMS Credits, Billing Requests, Auth/Permissions.
- Messaging send tools intentionally deferred — SMS quiet-hours/cap/opt-out/provider-failure checks only become required if a send tool is added (out of scope Phases 0–4).

## Guardrails
- Do not modify unlisted files.
- Do not refactor unrelated code.
- Do not rename public APIs unless explicitly listed.
- Do not change database schema unless explicitly listed (and only after verifying `auditLogs.action` storage).
- Do not alter auth/permissions unless explicitly listed.
- Do not infer missing details.
- Do not add send-message, delete, bulk, import-commit, or billing-payment tools (see Done Criteria exclusions).
- Do not enable `openai_native_assistant_mutations_enabled` until Phase 0 audit logging lands.

## Implementation Order
- Phase 0 (prereq, approved): enable native path non-prod (stream→tools); add audit logging to `assistant-mutation.service.ts` + extend `AuditAction`; verify legacy fallback. Accept: existing specs pass on native path; every confirmed mutation writes an audit row.
- Phase 1 (approved): add 6 read tools (openai-tools + tool-policy + read-model + help.module + `listRecentRuns`). Risk LOW.
- Phase 2 (approved): add 5 diagnostic tools + FE diagnostic card + next-actions list. Risk LOW.
- Phase 3 (NEEDS APPROVAL): `update_automation_settings` / `create_sms_topup_request` draft-only. Risk MEDIUM.
- Phase 4 (NEEDS APPROVAL): confirm path for the two new actions + audit + dedupe. Risk HIGH.

## Verification Commands
- `bun run test`
- `bun run typecheck`
- `bun run lint`
- `bun run check:assistant-context-governance`
(Source: `docs/ai/architecture-manifest.md:80-89`.)

## Manual Verification Flow
1. With native flags on in non-prod, open the assistant; ask a usage/metrics question → confirm tool path (logs show `openai.responses.tool.requested`).
2. Ask "what should I do today?" → `get_recommended_next_actions` returns prioritized chips to whitelisted routes.
3. Ask "why didn't the reminder send?" → `diagnose_reminder_issue` returns `{reason,evidence,fixRoute}`, no raw provider strings.
4. Phase 4 only: trigger `update_automation_settings` → confirmation card → confirm → verify mutation applied AND an `auditLogs` row written; replay/stale/cross-tenant rejected.

## Rollback / Risk Notes
- All phases are flag-gated; rollback = flip the env flag off → legacy path resumes.
- Keep legacy `chatStream` path intact until native path is proven in prod.
- Mutations flag stays off until Phase 0 audit logging verified.

## Done Criteria
- Phases 0–2: native path live, 6 reads + 5 diagnostics functioning, PII-safe, all specs/typecheck/lint green; every existing confirmed mutation now writes an audit row.
- Phases 3–4 (separate approval): two new confirmed actions live behind confirm+audit; stale/replay/cross-tenant/read-only all rejected.
- Explicitly NOT done here (do not implement): delete/bulk-delete customers; any send-message tool; bulk messaging/mutations; import commit via assistant; auto-change plan/checkout/cancel/resume; refund/approve-billing-request/void-payment; direct automation-rule mutation without proposal+confirm+audit; appointment create/cancel.
- Open product decisions to resolve before relevant phase: flag rollout cohort; model upgrade vs gpt-4o-mini; `getBusinessHours`/`getServices` source (unverified); public booking-disabled flag (unverified); `AuditAction` naming sign-off.
