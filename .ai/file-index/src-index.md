# Source Index

| File Path | Purpose | Relationships | Usage Patterns |
|---|---|---|---|
| `apps/web/src/app/layout.tsx` | Web root layout and providers wiring | Uses `providers.tsx`, shared UI | Entry point for all web routes |
| `apps/web/src/app/(dashboard)/layout.tsx` | Dashboard shell composition | Uses `RequireSession` and navigation components | Shared protected frame for authenticated app pages |
| `apps/web/src/app/onboarding/layout.tsx` | Onboarding shell composition | Uses `RequireSession`, auth sync, workspace provider, and auth button | Shared protected frame for onboarding/setup pages |
| `apps/web/src/lib/plan-capabilities.ts` | Plan-to-web-visibility helper | Consumed by dashboard shell, analytics, settings, and AI UI surfaces | Maps current billing plan to AI visibility booleans without introducing a second provider flag |
| `apps/web/src/hooks/use-plan-capabilities.ts` | Client hook for plan-based AI visibility | Wraps billing status and plan-capabilities helper | Centralizes plan-aware hide/show logic for assistant, AI usage, analytics, and refine actions |
| `apps/web/src/proxy.ts` | Next middleware pass-through | Defers auth decisions to client/API session guard and backend API guards | Avoids rejecting API-origin session cookies at the web-origin middleware boundary |
| `apps/api/src/main.ts` | Nest application bootstrap | Loads `AppModule`, global pipe/filter, and auth bootstrap service | API process entry point including optional default-account initialization |
| `apps/api/src/app.module.ts` | Top-level API module composition | Imports domain modules and shared modules | Central dependency wiring |
| `apps/api/src/auth/clerk-auth.guard.ts` | First-party session guard | Used by protected API controllers | Resolves tenant context, including `clerkId`, from canonical session cookie, temporary legacy cookie, or bearer token |
| `apps/api/src/platform-admin/*` | Platform-admin authorization/session/manual billing/communications module | Uses first-party auth, platform-admin RBAC schema, shared permission types, billing catalog, shared add-on grant services, and existing messaging audit tables | Denies by default, requires an active platform admin row, resolves database-backed permissions, serves `/platform-admin/session`, and exposes manual billing/payment/SMS adjustment plus communications monitoring endpoints |
| `apps/web/src/app/(platform-admin)/platform-admin/*` | Platform-admin route group | Uses platform-admin guard/shell components and platform-admin feature pages | Internal `/platform-admin` pages protected by first-party session plus platform-admin RBAC session check; includes overview, businesses, billing requests, communications, and audit logs |
| `apps/api/src/billing/*` | Billing catalog, Lemon Squeezy integration, webhook ingestion, status endpoints, and shared add-on grant services | Uses shared plan types, database subscription/webhook tables, ledger tables, and owner/tenant guards | Server-side variant resolution, checkout creation, webhook signature validation, provider-neutral add-on fulfillment, and billing status orchestration with self-serve feature gating and free-cap fallback when disabled |
| `apps/api/src/test/deploy-workflow-governance.spec.ts` | Deploy workflow regression guard | Reads `.github/workflows/deploy.yml` from repo root | Verifies cold rebuild timeout settings, stale dev/prod Compose cleanup without volume deletion, and public web smoke checks for dev artifacts/localhost API URLs |
| `apps/api/src/appointments/*` | Appointment booking and lifecycle API/service/scheduler | Used by dashboard appointments page and automation scheduler | Staff arrival, Needs Review, duration snapshots, idempotent visit recording, post-visit/loyalty side effects, and feature-flagged lifecycle reconciliation |
| `apps/api/src/intake/intake-booking.service.ts` | Intake slot availability, hold, and provider-aware OTP confirmation logic | Called by intake controller and uses DB schema plus OTP providers | Prevents slot conflicts, confirms booking intent via Twilio Verify or local Semaphore OTP hash verification, persists appointment duration snapshots, and records public-safe blocked-send audit outcomes when billing/provider sends are gated off |
| `apps/api/src/intake/otp/*` | Booking OTP provider abstraction, Twilio/Semaphore providers, and failover state | Used by `IntakeBookingService` and `IntakeModule` | Supports `OTP_PROVIDER_MODE`, organization-scoped persistent failover, Semaphore-generated Tyvera OTP hashes, and Twilio Verify fallback during migration |
| `apps/api/src/messaging/*` | Messaging provider, webhook, dispatch, metering, and manual SMS safety-net logic | Uses Semaphore/Twilio/Resend providers, metering tables, automation policy, scheduler, manual follow-up task table, and processed webhook events | Explicit SMS provider selection, Semaphore outbound automation sends/reconciliation, secure Twilio webhook validation, Resend Svix idempotency, TwiML inbound responses, segment-aware SMS credits, safe retry behavior, provider delivery status updates, manual follow-up task creation/digest/retry APIs for unconfirmed appointment SMS |
| `apps/web/src/components/needs-attention/*` | Needs Attention manual follow-up UI modules | Used by dashboard route and adaptive navigation badge | API helpers, native SMS link creation, shared dashboard header/status/skeleton/action primitives, slate/blue task cards, and page state for reviewing SMS reminders that need staff attention |
| `apps/web/src/components/tyvera-assistant.tsx` | Global floating assistant panel entrypoint for dashboard surfaces | Integrated by adaptive app shell and powered by help-content + AI usage APIs | Guided chat + usage transparency UX for non-technical users, hidden entirely on free/starter plans |
| `apps/web/src/components/billing/*` | Billing presentation components | Used by `/pricing` and `/settings/billing` | Plan comparison, billing state banners, usage meters, add-on options, and checkout-sync UI |
| `packages/database/src/index.ts` | DB package public exports | Re-exports schema/db helpers | Imported by API services |
| `packages/types/src/index.ts` | Shared type and runtime contract exports | Imported by API/web/database packages | Includes Philippine mobile helpers plus shared billing plan/provider/status, add-on SKU/source, manual billing/payment status, and platform-admin role/permission contracts used across API, web, and DB layers |
