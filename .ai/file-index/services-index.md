# Services Index

| File Path | Purpose | Relationships | Usage Patterns |
|---|---|---|---|
| `apps/api/src/auth/auth.service.ts` | First-party auth service (public sign-up OTP + password login/reset + sessions) | Used by `auth.controller` and `ClerkAuthGuard` compatibility guard | Issues/verifies sign-up and password-reset OTP challenges, stores/replaces password hashes, validates password credentials, revokes old sessions after reset, creates sessions, resolves signed-in user context including `clerkId`, and computes post-login onboarding/dashboard redirect metadata |
| `apps/api/src/auth/auth.bootstrap.service.ts` | Startup default-account bootstrap service | Invoked from API startup path | Ensures env-configured test owner account exists without overwriting existing identity |
| `apps/api/src/customers/customers.service.ts` | Customer domain logic | Called by customers/intake controllers and appointment booking UI through API | Customer CRUD and legacy visit logic; resolves booking customers by exact normalized `(businessId, mobile)` inside an advisory lock without overwriting existing profiles; persists optional mobile as blank/null or normalized Philippine E.164 `+639...` values |
| `apps/api/src/billing/billing.service.ts` | Billing application logic | Calls Lemon Squeezy, billing catalog helpers, database ledger tables, AI/email usage sources, and webhook event storage | Billing catalog/status responses, hosted checkout creation, portal redirects, webhook-authority-only plan/cancel/resume lifecycle requests with pending-sync UX metadata, feature-flagged Lemon no-op fallback when self-serve billing is disabled, direct current-month UTC ledger lookup by `(organizationId, month)`, subscription/order webhook reconciliation with processed/ignored/failed event storage, verified-booking/SMS add-on balance and refund-unit updates, email and AI usage status assembly, unresolved-only refund-review warnings, delayed-sync warning surfacing, refund handling, and ignored-event recording |
| `apps/api/src/billing/sms-addon-grant.service.ts` | Provider-neutral SMS add-on grant service | Used by Lemon Squeezy order webhooks and platform-admin manual billing/adjustments | Resolves current-month SMS ledger, enforces idempotency by `(source, sourceReference)`, increments or corrects add-on balance, inserts `sms_addons`, and writes credit reconciliation events |
| `apps/api/src/billing/verified-booking-addon-grant.service.ts` | Provider-neutral verified-booking add-on grant service | Used by Lemon Squeezy order webhooks and platform-admin manual billing | Resolves current-month verified-booking ledger, enforces idempotency by `(source, sourceReference)`, increments add-on balance, inserts `verified_online_booking_addons`, and writes credit reconciliation events |
| `apps/api/src/billing/lemonsqueezy.service.ts` | Lemon Squeezy provider client and webhook validator | Used by billing controller/service and billing webhook controller | Creates hosted checkout URLs, fetches fresh subscription portal URLs, updates/cancels subscriptions, and validates raw-body `X-Signature` HMACs with timing-safe comparison |
| `apps/api/src/billing/plan-catalog.ts` | Canonical plan and add-on catalog | Shared by billing, capacity, and web pricing surfaces | Free/starter/growth/pro definitions, growth booking override, and allowlisted variant env-key resolution |
| `apps/api/src/billing/credit-reconciliation.ts` | Credit ledger helper logic | Used by billing tests and future webhook/usage reconciliation flows | Deterministic included-plus-addon-minus-used remaining calculations and upgrade allowance math |
| `apps/api/src/billing/billing-env.ts` | Billing/runtime configuration validation | Called during API bootstrap | Fails fast on required Lemon Squeezy or Twilio Verify env gaps only when self-serve billing is enabled; stays permissive in free-cap mode |
| `apps/api/src/platform-admin/platform-admin.service.ts` | Platform-admin RBAC session resolver | Used by platform-admin guard/controller | Loads active platform admin rows, joins assigned roles and permissions, and serializes `/platform-admin/session` response data |
| `apps/api/src/platform-admin/platform-admin-billing.service.ts` | Platform-admin manual billing and credit orchestration | Uses platform-admin permissions, billing catalog, manual billing tables, shared grant services, and audit logs | Lists organizations/requests/audit logs, creates catalog-snapshotted manual billing requests, records payments, confirms exact payments in a transaction, prevents duplicate fulfillment, rejects/voids requests, and applies SMS promotional grants/corrections |
| `apps/api/src/messaging/messaging.service.ts` | Message generation and dispatch logic | Uses AI execution, providers, metering | Message creation/send orchestration |
| `apps/api/src/messaging/message-dispatch.service.ts` | Automated message dispatch orchestration | Uses plan capacity, automation policy, SMS/email providers, metering services, and manual follow-up service | Finalizes message content, estimates SMS segments, enforces segment-aware credits, persists dynamic SMS provider metadata on success and final failure, omits Twilio STOP copy for Semaphore sends, retries only provider-marked safe SMS failures, and creates manual follow-up tasks for supported appointment SMS final failures |
| `apps/api/src/messaging/manual-follow-ups/manual-follow-up.service.ts` | Manual SMS follow-up task service | Uses message events, businesses, customers, appointments, and manual follow-up task rows | Creates idempotent one-task-per-original-event safety-net rows for supported appointment SMS failures, lists open tasks with duplicate-risk metadata, counts open tasks, resolves/dismisses tasks, links staff retry message events, and marks digest notifications |
| `apps/api/src/messaging/manual-follow-ups/manual-follow-up-retry.service.ts` | Staff-triggered automatic SMS retry orchestration | Uses `ManualFollowUpService` and `MessageDispatchService` | Blocks unknown-outcome duplicate-risk retries, sends retry attempts through the normal dispatch/metering path, preserves original failed message events, and links successful retry event IDs to manual tasks |
| `apps/api/src/messaging/manual-follow-ups/manual-follow-up-digest.service.ts` | Owner digest sender for manual SMS follow-up tasks | Uses `IEmailProvider`, owner auth identities, and manual follow-up tasks | Sends privacy-safe owner digest emails containing only counts and `/needs-attention` link, then marks tasks notified only after at least one owner email succeeds |
| `apps/api/src/messaging/semaphore-message-reconciliation.service.ts` | Semaphore delivery reconciliation job | Uses Semaphore message status API, message events, and manual follow-up service | Polls recent accepted Semaphore appointment SMS events, maps `Queued`/`Pending`/`Sent`/`Failed`/`Refunded`, updates delivery status, and creates idempotent manual tasks for delayed failed/refunded states |
| `apps/api/src/messaging/providers/semaphore-sms.provider.ts` | Semaphore outbound SMS provider | Registered by `MessagingModule` behind `SMS_PROVIDER=semaphore` | Sends regular Semaphore messages, normalizes Philippine E.164 numbers to Semaphore format, returns provider IDs/metadata, and maps transient/rejected/unknown outcomes |
| `apps/api/src/messaging/messaging-webhook.service.ts` | Trusted provider delivery event processing | Called by messaging webhook controllers after signature validation | Maps Twilio/Resend delivery states, preserves terminal statuses, and stores raw provider metadata/failure reasons |
| `apps/api/src/messaging/twilio-webhook-validation.service.ts` | Shared Twilio webhook signature validator | Used by inbound SMS and status webhook controllers | Validates complete form params using Twilio's helper library and explicit public callback URL env vars with local-only fallback |
| `apps/api/src/messaging/sms-metering.service.ts` | SMS monthly/add-on segment metering | Used by SMS dispatch and billing status flows | Resolves current UTC month rows directly by `(organizationId, month)`, consumes included before add-on segment credits, and preserves add-on balances across month rollover |
| `apps/api/src/messaging/email-metering.service.ts` | Email monthly cap metering and usage tracking | Used by `MessageDispatchService`, `MessagingController`, and `PlanCapacityService` | Enforces plan-catalog-driven monthly email caps, resolves current UTC month rows directly by `(organizationId, month)`, and records per-send usage |
| `apps/api/src/ai/ai-execution.service.ts` | Guardrailed AI execution wrapper | Used by messaging and AI module | AI policy-bound text generation with structured request/success/error logging for OpenAI actions/responses, plus optional OpenAI `response_format` JSON-schema enforcement for deterministic assistant outputs |
| `apps/api/src/ai/ai-usage.service.ts` | AI usage accounting, summary, and budget checks | Used by AI controller/services and assistant usage UI endpoints | Computes monthly + daily usage limits, reset windows, and deterministic budget block reasons (`AI_DAILY_CAP_EXCEEDED`, etc.) |
| `apps/api/src/imports/imports.service.ts` | Import pipeline orchestration | Uses mapping/migration/provider adapters and shared mobile validation helpers | Batch import processing; commit rejects invalid nonblank mobile instead of silently normalizing loose formats |
| `apps/api/src/intake/intake-booking.service.ts` | Intake booking and OTP flow | Uses OTP failover providers, org billing state, verified-booking credit tables, OTP send audit rows, shared timing helper, and shared mobile validation helpers | Public booking journey backend requiring valid Philippine E.164 mobile for OTP-backed holds, duration snapshot on confirmed appointments, safe billing-blocked failures with blocked-send `public_otp_send_events.outcome` audit codes, provider-aware resend-window reuse, resend cooldown / per-hold send caps, rolling per-mobile/IP caps, per-business daily caps, stable public verify errors, Twilio/Semaphore provider state capture, local Semaphore hash verification, post-send verified-booking credit consumption, and feature-flag aware free-mode fallback when billing is disabled |
| `apps/api/src/appointments/appointments.service.ts` | Appointment lifecycle and booking domain logic | Used by appointments controller and lifecycle scheduler; composes automation send and intake booking service | Creates staff/override appointments with duration snapshots, marks Arrived, lists Needs Review, idempotently completes appointments and records customer visits once via `visitRecordedAt`, triggers post-visit/loyalty side effects once, preserves missed recovery, blocks rescheduling recorded visits, and reconciles due lifecycle rows in bounded batches while logging per-row failures |
| `apps/api/src/appointments/appointment-lifecycle-scheduler.service.ts` | Appointment lifecycle cron scheduler | Uses feature flags and appointments service under Nest ScheduleModule | Every five minutes, runs lifecycle reconciliation only when `FF_appointment_visit_automation_enabled` is enabled |
| `apps/api/src/intake/otp/otp-failover.service.ts` | Booking OTP provider failover orchestration | Uses Twilio Verify provider, Semaphore OTP provider, and `otp_provider_settings` | Resolves `OTP_PROVIDER_MODE`, reads persisted organization OTP provider state, persists Semaphore failover for allowlisted permanent Twilio failures, and retries the same send once with Semaphore |
| `apps/api/src/intake/otp/otp-provider-settings.service.ts` | Persistent OTP provider state service | Uses database `otp_provider_settings` rows | Reads organization OTP provider mode and idempotently switches organizations to Semaphore with reason/timestamp metadata |
| `apps/api/src/intake/otp/semaphore-otp.provider.ts` | Semaphore booking OTP provider | Used by OTP failover and intake verification | Generates secure numeric OTPs, stores only PBKDF2 hashes, sends Semaphore OTP messages, verifies hashes locally with expiration and timing-safe comparison, and maps provider failures |
| `apps/api/src/intake/otp/twilio-verify-otp.provider.ts` | Twilio Verify booking OTP provider | Used by OTP failover and legacy Twilio hold verification | Wraps Twilio Verify send/check calls, maps transient provider failures, and marks only configured Twilio error codes as failover-eligible |
| `apps/api/src/help/answer-source.service.ts` | Normalized source-grounded answer envelope assembly | Composes insights, SMS metering, billing, and AI usage services | Read-only assistant prep data responses with deterministic fallbacks, including monthly + daily AI usage canonical fields for assistant/live-usage UI consumers |
| `apps/api/src/help/assistant.service.ts` | OpenAI-native assistant orchestration runtime with staged intent-tool pipeline shared by chat + stream | Composes `AiExecutionService`, `AnswerSourceService`, and `assistant-context` markdown context packs | Deterministic stages (`normalize -> intent -> context -> tools -> openai -> validate -> finalize`), strict JSON-schema response contract + bounded legacy normalization (`action/url -> href`, string confidence mapping), canonical intent-tool registry enforcement, validation diagnostics (`schema/tool/route/confidence`), safe action-chip outputs, and SSE `usage` event emission before `done` for live bar updates |
| `apps/api/src/help/assistant-context.ts` | Layered assistant context-pack builder with query-aware ranking | Used by `assistant.service` and markdown loader | Builds token-budgeted `core/task/data/memory/tools` slices from `docs/assistant-context/*.md`, ranking entries by intent + route/action keyword match before priority fallback |
| `apps/api/src/help/assistant-thread-memory.service.ts` | Persistent thread memory store for assistant conversations | Uses database schema table `assistant_thread_memories` | Loads/saves rolling summary and last N turns by org/user/thread id |
| `apps/api/src/security/pii-crypto.service.ts` | PII encryption/decryption utilities | Used by security/privacy services | Sensitive data protection |
