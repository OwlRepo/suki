# Components Index

| File Path | Purpose | Relationships | Usage Patterns |
|---|---|---|---|
| `apps/web/src/components/navigation/adaptive-app-shell.tsx` | Adaptive dashboard shell | Used by dashboard layout/navigation | Responsive app navigation wrapper that loads manual follow-up open-count badges and gates assistant rendering by plan visibility |
| `apps/web/src/components/navigation/dashboard-nav-config.ts` | Dashboard navigation source config | Consumed by desktop/mobile navigation components | Defines grouped sidebar links and mobile bottom-nav priorities with Appointments as the customer-facing daily workflow, excluding standalone Customers links while retaining Needs Attention badges and AI analytics nav suppression on unsupported plans |
| `apps/web/src/components/needs-attention/*` | Manual follow-up review UI | Used by `/needs-attention` dashboard route and navigation badge API calls | Uses shared dashboard primitives (`PageHeader`, `StatusBanner`, `ListSkeleton`, `PrimaryPageAction`) with slate/blue task cards to list open SMS safety-net tasks, copy message text, open native SMS deep links, block automatic retry for duplicate-risk unknown outcomes, and resolve/dismiss tasks |
| `apps/web/src/components/platform-admin/*` | Internal platform-admin guard, shell, API helper, and types | Used by `/platform-admin` route group | Verifies `/platform-admin/session`, redirects unauthorized users, and renders a minimal internal shell without workspace dropdown, trial banner, onboarding wrapper, or assistant |
| `apps/web/src/components/platform-admin/overview/platform-admin-overview-page.tsx` | Platform-admin overview | Uses platform-admin session API, communications summary API, operations overview API, and shared UI primitives | Shows loading/error states, refresh action, active platform-admin user ID, roles, status, permission count, live communications failure/follow-up cards for users with `COMMUNICATION_VIEW`, and provider-health/critical-alert/failed-run cards for users with operations permissions |
| `apps/web/src/components/platform-admin/businesses/*` | Platform-admin organization billing, subscription, contact, and credit UI | Used by `/platform-admin/businesses*` routes | Preserves organization search, metrics, ledgers, histories, add-on requests, and SMS adjustments while adding canonical monthly subscription requests, billing-contact editing, lifecycle confirmation, validation, loading/success/error states, disabled-controls review mode, and payment-request sent/missing-recipient/provider-failure feedback with copy fallback |
| `apps/web/src/components/platform-admin/billing/*` | Platform-admin manual billing request and billing-email UI/API contracts | Used by `/platform-admin/billing-requests*` routes and business detail | Preserves add-on request/payment/fulfillment/audit behavior, adds manual catalog and subscription snapshot contracts, displays plan/interval/coverage, feature-gates mutations, renders delivery history and request/acknowledgment resend states, explains the request Pro Forma Invoice attachment, and keeps copyable payment instructions/acknowledgments available after email failures |
| `apps/web/src/components/platform-admin/communications/*` | Platform-admin communications monitoring UI | Used by `/platform-admin/communications` route | Loads communications summary/list/detail APIs, renders summary cards, filters, Recharts line charts, masked-recipient message cards, and a Sheet detail drawer for provider/message/manual-follow-up investigation |
| `apps/web/src/components/platform-admin/operations/*` | Platform-admin operations monitoring UI | Used by `/platform-admin/automation-runs` and `/platform-admin/alerts` routes plus overview cards | Loads automation-run, provider-health, and alert APIs; renders provider cards, scheduler summaries, alert summaries, filters, loading/error/empty states, and acknowledge/resolve actions with disabled submitting buttons |
| `apps/web/src/components/platform-admin/audit/*` | Platform-admin audit-log UI | Used by `/platform-admin/audit-logs` route | Loads sensitive platform-admin audit events with loading/error/empty/refresh states |
| `apps/web/src/components/require-session.tsx` | Client session gate for protected shells | Uses `use-session` and Next router | Shows session-loading state, redirects signed-out users to `/sign-in`, and prevents protected content flash |
| `apps/web/src/components/share-slots/share-slots-workflow.tsx` | Share-slots workflow surface | Uses workspace context, auth hooks, and appointments share-template APIs | Step-by-step slot sharing flow with template management, asset generation, and copy actions |
| `apps/web/src/components/ai-quota-banner.tsx` | AI usage and quota awareness banner | Rendered by dashboard surfaces that expose AI actions | Surfaces policy/usage state only for AI-enabled plans before AI actions |
| `apps/web/src/components/dashboard-nav.tsx` | Primary dashboard nav links | Depends on dashboard nav config | Route-level navigation |
| `apps/web/src/components/auth-button.tsx` | Signed-in sign-out button | Uses auth client, session cache invalidation, and router refresh | Clears backend session, invalidates client session cache, redirects home, and refreshes route state |
| `apps/web/src/components/onboarding/onboarding-wizard.tsx` | Onboarding flow UI | Uses onboarding hooks/services | Progressive onboarding state |
| `apps/web/src/components/onboarding/onboarding-dashboard-preview-step.tsx` | Onboarding dashboard preview copy | Used by onboarding wizard | Appointment-first preview actions: schedule appointment, mark Arrived, and let Tyvera record visits automatically |
| `apps/web/src/components/ui/date-pickers.tsx` | Shared shadcn-style date selection primitives | Used by appointments, intake, and onboarding flows with popover/calendar interactions | Replaces native `date`/`month`/`datetime-local` inputs using `DatePicker`, `MonthPicker`, and `DateTimePicker` |
| `apps/web/src/components/ui/availability-calendar.tsx` | Shared availability calendar grid | Used by appointments booking and intake scheduling flows | Compact 7-column month calendar with tap-friendly day chips, disabled unavailable dates, and mobile-first reduced vertical height |
| `apps/web/src/components/customers/customer-form-modal.tsx` | Customer create/edit modal | Calls API via lib/hook layer and shared Philippine mobile helpers | CRUD workflow UI with `+639171234567` placeholder/helper text and invalid nonblank mobile blocked before submit |
| `apps/web/src/components/billing/plan-comparison-grid.tsx` | Pricing grid for plans and add-ons | Used by `/pricing` and billing settings route | Renders free/starter/growth/pro cards, billing CTAs, disabled-helper copy for feature-gated paid billing, and most-popular emphasis |
| `apps/web/src/components/billing/billing-interval-toggle.tsx` | Billing interval segmented control | Used by pricing/billing pages | Toggles monthly vs annual presentation without changing server authority |
| `apps/web/src/components/billing/current-plan-card.tsx` | Current subscription summary panel | Used by `/settings/billing` | Shows plan, interval, renewal/cancel metadata, and billing actions |
| `apps/web/src/components/billing/usage-meter-card.tsx` | Usage-meter summary card | Used by `/settings/billing` | Displays included/add-on/used/remaining values with accessible labels |
| `apps/web/src/components/billing/checkout-sync-banner.tsx` | Checkout return-state banner | Used by `/settings/billing` | Shows activation-pending, delayed-sync, and retry guidance after checkout redirect; hidden when self-serve billing is disabled |
| `apps/web/src/components/billing/billing-status-banner.tsx` | Persistent billing-state banner | Used by `/settings/billing` | Surfaces cancellation, past-due, paused, and scheduled-change warnings |
| `apps/web/src/components/billing/addon-pack-grid.tsx` | Add-on purchase option grid | Used by pricing/billing pages | Lists OTP booking and SMS segment top-up packs with CTA buttons |
| `apps/web/src/components/insights/insights-charts.tsx` | Insights chart rendering | Consumes analytics data contracts | Dashboard analytics visualizations |
| `apps/web/src/components/assistant/assistant-markdown.tsx` | Restricted assistant-only Markdown renderer | Used by Tyvera Assistant response and detail bodies with `react-markdown`, GFM, and Next links | Renders compact headings, emphasis, lists, tables, code, blockquotes, and rules; skips raw HTML and allows clickable links only for fixed internal Tyvera routes |
| `apps/web/src/components/assistant/use-progressive-assistant-text.ts` | Client-side progressive assistant text buffer | Used only by real streamed assistant reply bubbles | Drains complete SSE/done target text in small timed batches, bypasses animation for reduced motion or disabled callers, reports reveal state/progress, and cancels timers on cleanup |
| `apps/web/src/components/tyvera-assistant.tsx` | Global assistant launcher and compact chat/action panel UX | Mounted by adaptive app shell; consumes auth/workspace/help/usage utilities, assistant Markdown/progressive helpers, confirmation UI, and `api-base` | Renders assistant-only restricted Markdown while keeping user text plain, progressively drains streamed targets and final next steps, keeps loading/read state truthful, follows the bottom only while the owner stays near it, renders typed server-owned draft notices, preserves `/chat` fallback only for transport/protocol failure, distinguishes disabled AI from quota caps, applies streamed usage state, and requires explicit signed-mutation confirmation |
| `apps/web/src/components/ui/button.tsx` | Shared UI primitive | Used across feature components | Reusable design-system control |
| `packages/ui/src/Button.tsx` | Workspace-level UI button | Shared package for cross-app reuse | Foundation UI component |
