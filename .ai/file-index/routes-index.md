# Routes Index

| File Path | Purpose | Relationships | Usage Patterns |
|---|---|---|---|
| `apps/web/src/app/page.tsx` | Public landing route | Uses landing components | Anonymous entry route |
| `apps/web/src/app/(dashboard)/layout.tsx` | Protected dashboard route shell | Wraps dashboard navigation with `RequireSession` | Blocks signed-out dashboard content render and redirects through client/API session state |
| `apps/web/src/app/(dashboard)/dashboard/page.tsx` | Main app dashboard | Uses navigation and metrics components | Authenticated appointment-first primary route with CTAs into `/appointments` |
| `apps/web/src/app/(dashboard)/customers/page.tsx` | Legacy customer route redirect | Protected route policy and Next redirect | Redirects old `/customers` bookmarks to `/appointments`; customer records remain backend/internal |
| `apps/web/src/app/(dashboard)/imports/page.tsx` | Data import route | Calls imports API flow and uses `+639...` examples | Batch/mapping import workflows with Philippine E.164 mobile guidance |
| `apps/web/src/app/intake/[businessId]/page.tsx` | Public intake booking route | Uses intake error utils, wizard-progress helper, `api-base`, shared form/button/date-picker/availability-calendar primitives, and shared Philippine mobile helpers | External customer intake flow with production `/api` proxy calls, timeline-style wizard steps, month calendar availability, time slot selection, booking review, OTP verification, resend cooldown / hold-expiry countdown UI, slot-conflict restart handling, and strict `+639...` mobile validation before detail save or OTP hold |
| `apps/web/src/app/(dashboard)/appointments/page.tsx` | Appointment-first daily workspace | Integrates appointments lifecycle APIs, customer booking resolver, shared date-picker/availability-calendar primitives, status banners, skeletons, and empty states | Staff booking operations with month calendar, selected-day agenda, Arrived action, Needs Review queue, card-level pending actions, retry-safe reads, background-refresh failure preservation, and new-customer mobile validation before continuing |
| `apps/web/src/app/(dashboard)/share-slots/page.tsx` | Dedicated slot-sharing route | Renders share-slots workflow component and template APIs | Guided flow for composing daily slot visuals, captions, and booking links |
| `apps/web/src/app/(dashboard)/needs-attention/page.tsx` | Protected manual follow-up route | Renders needs-attention components under dashboard layout/session shell | Mobile-first owner/staff review queue for unconfirmed appointment SMS reminders with native SMS composer links, clipboard fallback, retry, contacted, and dismiss actions |
| `apps/web/src/app/(platform-admin)/platform-admin/layout.tsx` | Protected platform-admin route shell | Wraps platform-admin pages with `RequireSession`, `RequirePlatformAdmin`, and internal shell | Blocks signed-out users, redirects authenticated non-platform users to `/dashboard`, and renders internal navigation only after platform-admin session verification |
| `apps/web/src/app/(platform-admin)/platform-admin/page.tsx` | Platform-admin overview route | Renders platform-admin overview component | Initial internal overview placeholder backed by live `/platform-admin/session` data |
| `apps/web/src/app/(platform-admin)/platform-admin/businesses/page.tsx` | Platform-admin businesses route | Renders platform-admin business list component | Searches tenant organizations and links to organization-level billing/credit detail |
| `apps/web/src/app/(platform-admin)/platform-admin/businesses/[organizationId]/page.tsx` | Platform-admin business detail route | Renders platform-admin business detail component | Shows ledgers/history and exposes billing request plus SMS adjustment actions |
| `apps/web/src/app/(platform-admin)/platform-admin/billing-requests/page.tsx` | Platform-admin billing-request list route | Renders platform-admin billing requests component | Filters manual billing requests by lifecycle status |
| `apps/web/src/app/(platform-admin)/platform-admin/billing-requests/[billingRequestId]/page.tsx` | Platform-admin billing-request detail route | Renders platform-admin billing request detail component | Shows request/payment/fulfillment/audit state and finance actions |
| `apps/web/src/app/(platform-admin)/platform-admin/audit-logs/page.tsx` | Platform-admin audit-log route | Renders platform-admin audit logs component | Lists sensitive platform-admin actions for manual billing and credit changes |
| `apps/web/src/app/sign-in/page.tsx` | Custom first-party sign-in route | Uses `auth-client` password API, session cache invalidation, router refresh, and links to `/forgot-password` | Email/password sign-in without OTP on login; follows API redirect metadata to `/onboarding` or `/dashboard` |
| `apps/web/src/app/forgot-password/page.tsx` | Custom first-party password reset route | Uses `auth-client` password reset APIs, session cache invalidation, and router refresh | Separate email OTP recovery flow with enumeration-safe send copy, new-password verification, automatic session creation, and onboarding/dashboard redirect |
| `apps/web/src/app/sign-up/page.tsx` | Custom first-party sign-up route | Uses `auth-client` sign-up OTP/password APIs, session cache invalidation, and router refresh | Two-step email/password signup, OTP email verification, and immediate onboarding redirect |
| `apps/web/src/app/onboarding/layout.tsx` | Protected onboarding shell | Wraps onboarding workspace setup with `RequireSession` | Blocks signed-out onboarding content render before auth sync/workspace loading |
| `apps/web/src/app/(dashboard)/help/page.tsx` | Help Center route with unified search and onboarding replay | Uses help-content indexing and shared UI cards/buttons | Client self-serve docs, quick answers, and guided onboarding replay |
| `apps/web/src/app/onboarding-demo/page.tsx` | Sales/client guided onboarding demo route | Reuses onboarding guide modules from help-content | Step-by-step non-destructive walkthrough for demos and re-training |
| `apps/web/src/app/(dashboard)/settings/page.tsx` | Settings route (organization, business, automation, messaging, AI) | Calls organizations/businesses/automation/messaging/AI APIs | Workspace configuration with free-default copy, usage caps, automation template editing with default-filled fields plus loading/success/error blur autosave status, and plan-gated hiding of AI usage/quota surfaces and `Refine with AI` actions on free/starter plans |
| `apps/web/src/app/(dashboard)/analytics/page.tsx` | Usage analytics route | Calls insights monitoring API and plan-capability helper | Shows AI and automation analytics for growth/pro plans and renders a locked upgrade state on unsupported plans |
| `apps/web/src/app/pricing/page.tsx` | Public pricing route | Uses billing plan/add-on presentation components plus first-party session hook | Freemium plan comparison with monthly/annual toggle, visible-but-disabled annual CTAs, signed-out vs signed-in billing CTA routing, and disabled paid-billing CTAs when self-serve billing is off |
| `apps/web/src/app/(dashboard)/settings/billing/page.tsx` | Protected billing settings route | Uses session/auth helpers plus billing UI components and API billing endpoints | Current-plan visibility, paused/cancellation/scheduled-downgrade billing banners, verified-booking/SMS/email/AI usage meters, checkout sync polling, staff read-only billing visibility, owner warning banners, and owner-facing billing actions for plan changes, portal access, cancellation, resume, and top-up checkout; hides Lemon-backed mutations when self-serve billing is disabled |
| `apps/api/src/customers/customers.controller.ts` | Customer REST endpoints | Delegates to customer services/templates and shared Philippine mobile helpers | CRUD + templates + legacy visit actions plus `POST /customers/resolve-for-booking` exact-mobile booking reuse; invalid nonblank mobile rejected |
| `apps/api/src/imports/imports.controller.ts` | Import REST endpoints | Delegates to imports/mapping services | Parse/validate/commit pipelines with invalid nonblank mobile rejected |
| `apps/api/src/billing/billing.controller.ts` | Billing REST endpoints | Uses billing/Lemon Squeezy services, tenant role context, and owner auth enforcement | Plan catalog, staff-vs-owner billing status shaping, hosted checkout, customer portal, and owner-only plan/cancel/resume mutations that return pending webhook sync while verified webhooks remain the final lifecycle authority; plan catalog also reports whether self-serve billing and annual checkout are enabled |
| `apps/api/src/messaging/messaging.controller.ts` | Messaging REST endpoints | Uses messaging + AI services | Generate/send usage and metering |
| `apps/api/src/messaging/manual-follow-ups/manual-follow-up.controller.ts` | Manual follow-up REST endpoints | Uses manual follow-up services and tenant auth | `GET /messaging/manual-follow-ups`, `GET /messaging/manual-follow-ups/open-count`, `PATCH /messaging/manual-follow-ups/:id`, and `POST /messaging/manual-follow-ups/:id/retry-sms` |
| `apps/api/src/automation/automation.controller.ts` | Automation REST endpoints | Uses automation settings + messaging services | Toggle, preview, template customization, and AI message refinement |
| `apps/api/src/intake/intake.controller.ts` | Public intake + booking REST endpoints | Delegates template, customer reuse, and booking hold/OTP logic to services and uses shared Philippine mobile helpers | External intake submission reuses exact business/mobile customers, availability lookup, slot holding, client-IP-aware OTP send limits with blocked-send audit outcomes, and OTP verification with stable public-safe error codes and valid `+639...` mobile required for holds |
| `apps/api/src/appointments/appointments.controller.ts` | Appointment REST endpoints | Delegates to `AppointmentsService` with tenant guards and billing write guard | Lists/creates/reschedules appointments, exposes staff `PATCH /:id/arrive`, manual completed/missed/cancelled status paths, Needs Review listing, reminders, share templates, booking holds/OTP/PIN override, and booking security |
| `apps/api/src/help/answer-source.controller.ts` | Read-only assistant-ready answer-source endpoints | Uses help answer-source service and auth guard | Source-grounded summaries for business metrics, SMS usage, billing, and AI usage |
| `apps/api/src/help/assistant.controller.ts` | OpenAI-native assistant chat orchestration endpoint | Uses assistant service with auth guard and tenant context | Plain-language assistant responses with action chips and confidence-safe fallbacks |
| `apps/api/src/platform-admin/platform-admin.controller.ts` | Platform-admin session/manual billing endpoints | Uses `ClerkAuthGuard`, `PlatformAdminGuard`, `PlatformAdminService`, and `PlatformAdminBillingService` | Exposes platform-admin session, organization lookup/detail, manual billing request, manual payment confirmation/rejection, SMS adjustment, and audit-log endpoints under `/platform-admin/*` |
