#!/usr/bin/env bun
/**
 * AI docs bootstrap helper (markdown-only).
 * Legacy Cursor .mdc generation is retired in favor of docs/ai.
 */

console.log("Markdown AI system is now canonical.");
console.log("Entry: AGENTS.md -> docs/ai/entry-point.md");
console.log("Legacy .cursor/.mdc workflow is retired.");
console.log("Use: bun run update:cursor-indexes to refresh docs/ai file indexes timestamps.");
