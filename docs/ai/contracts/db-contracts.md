# Database Contracts

Purpose:

Map important database models, ownership, and invariants.

This file is map only.

It is not proof of behavior.

Verify all conclusions against schema, migrations, services, jobs, and tests.

If this map conflicts with source code, source code wins.

Mark stale or conflicting entries as `CONTRACT DRIFT`.

Mark missing contracts as `UNMAPPED CONTRACT`.

## Contract Index

| Domain | Model / Table | Owner Module | Important Fields | Invariants | Mutation Paths | Transaction / Idempotency Rules | Related APIs / Jobs | Risk | Notes |
|---|---|---|---|---|---|---|---|---|---|
| Plan Upgrades | `organizations` | `apps/api/src/organizations`, `apps/api/src/billing`, `apps/api/src/platform-admin` | `billing_status`, `current_plan`, `billing_contact_*`, `preferred_payment_method`, `access_ends_at` | PK on `id`; enum-backed billing state fields verified in schema. Other business invariants: verify in services. | `organizations.controller.ts`, `billing.service.ts`, platform-admin billing services | `TODO: Fill after repository analysis. Do not treat as verified.` | `/organizations/me`, billing and platform-admin flows | Deep | Central billing state hub. |
| Settings / Onboarding | `businesses` | `apps/api/src/businesses`, `apps/api/src/onboarding` | `organization_id`, `business_type`, `crm_mode`, `workflow_profile` | FK to `organizations`; exact uniqueness rules need source verification. | `businesses.controller.ts`, onboarding services | No idempotency rule verified yet | Business settings and onboarding routes | Standard | Business profile drives domain-specific UX. |
| Auth / Permissions | `users`, `auth_identities`, `auth_otp_challenges`, `auth_sessions` | `apps/api/src/auth`, `apps/api/src/users` | `users.clerk_id`, `users.organization_id`, `users.role`, `users.active_business_id` | `users.clerk_id` unique verified in schema. Other auth invariants: verify services and tests. | `auth.service.ts`, `auth.bootstrap.service.ts`, `users.controller.ts` | OTP/session retry and expiry rules must be verified in auth source before change | `/auth/*`, `/users/me/workspace` | Deep | Auth truth. Never infer session or OTP rules. |
| Platform Admin | `platform_admins`, `admin_roles`, `admin_permissions`, `platform_admin_roles`, `admin_role_permissions`, `platform_admin_audit_logs` | `apps/api/src/platform-admin`, `apps/api/src/admin`, `apps/api/src/security` | role and permission linkage tables, admin status, audit timestamps | `platform_admins.user_id` unique verified in schema. Other RBAC invariants: verify seed and guard code. | `platform-admin.guard.ts`, platform-admin services, DB seed scripts | No global transaction policy verified yet | Platform-admin session, audit logs, billing review flows | Deep | Permission drift here can expose billing and ops routes. |
| Customers | `customers`, `customer_description_templates`, `business_default_description_templates`, `visit_adjustment_history` | `apps/api/src/customers`, `apps/api/src/security` | Template linkage fields, customer record fields in schema, visit adjustment history | `business_default_description_templates.business_id` is PK. Template FKs verified in schema. Full customer invariants: verify controller/service/tests. | `customers.service.ts`, customer template service, privacy controller | `TODO: Fill after repository analysis. Do not treat as verified.` | `/customers/*`, privacy correction/anonymize routes | Standard | Customer table fields not fully read in this bootstrap. Verify before schema-sensitive work. |
| Appointments | `appointments`, `appointment_share_templates`, `booking_security_settings`, `booking_pin_attempt_logs`, `booking_holds` | `apps/api/src/appointments`, `apps/api/src/intake` | appointment status enums, booking verification mode, share template fields, hold linkage | `appointment_share_templates` unique business/name constraint verified via migration `0019_appointment_share_templates.sql`; `booking_holds.customer_id` FK verified via `0018_booking_holds.sql` | `appointments.service.ts`, `intake-booking.service.ts`, booking endpoints | OTP, hold expiry, and idempotency rules must be verified in source | `/appointments/*`, `/intake/*`, lifecycle scheduler | Standard | Booking security paths deserve extra care even when task looks small. |
| Automations | `automation_settings`, `automation_job_runs` | `apps/api/src/automation`, `apps/api/src/operations` | automation toggles and content fields in schema, job run status enum | Job status enum verified in schema. Exact send invariants and dedupe rules: verify in services/specs. | `automation-settings.service.ts`, `automation-send.service.ts`, `automation-scheduler.service.ts`, `automation-job-run.service.ts` | Idempotency and replay rules not verified in bootstrap scan | `/automation/*`, platform-admin automation runs | Deep | Scheduler and send orchestration can fan out many messages. |
| Messaging | `message_events`, `manual_follow_up_tasks`, `processed_webhook_events`, `provider_health_snapshots`, `operations_alerts` | `apps/api/src/messaging`, `apps/api/src/operations` | message purpose/channel/status enums, delivery state, follow-up status, webhook processing markers | Enum-backed statuses verified in schema. Exact dedupe and webhook replay invariants: verify reconciliation and validation services. | `message-dispatch.service.ts`, `messaging-webhook.service.ts`, `semaphore-message-reconciliation.service.ts`, operations services | Provider webhook idempotency exists conceptually but exact keying must be verified in source | Messaging endpoints, webhooks, provider health, alerts | Deep | Includes inbound and outbound provider boundaries. |
| SMS Credits | `sms_credits`, `sms_usage_events`, `sms_addons`, `credit_reconciliation_events` | `apps/api/src/messaging`, `apps/api/src/billing`, `apps/api/src/platform-admin` | credit balance tables and usage events | Exact balance invariants not verified in bootstrap. Review reconciliation specs before planning. | `sms-metering.service.ts`, `sms-addon-grant.service.ts`, platform-admin SMS adjustment endpoints | Reconciliation/idempotency rules require source verification | `/messaging/credits`, billing add-on checkout, platform-admin SMS adjustments | Deep | Treat credit math as transaction-sensitive. |
| Email Usage | `email_credits`, `email_usage_events` | `apps/api/src/messaging` | email credit and usage fields | Exact invariants not verified yet | messaging services | `TODO: Fill after repository analysis. Do not treat as verified.` | Messaging usage views | Standard | Similar pattern to SMS metering. |
| Billing Requests | `manual_billing_requests`, `manual_billing_request_items`, `manual_payments`, `manual_billing_fulfillments`, `manual_billing_email_deliveries` | `apps/api/src/platform-admin`, `apps/api/src/billing` | manual billing status enum, payment status enum, payment method enum, reference numbers, item links | `manual_billing_requests.reference_number` unique verified via migration `0030_small_spot.sql`; `manual_billing_fulfillments` has unique item and source reference constraints verified in same migration | platform-admin billing services and controller actions | Manual payment confirmation and fulfillment linkage rules must be verified in source | Platform-admin billing request and payment endpoints | Deep | Strong candidate for transactional review before any mutation task. |
| Client Billing Requests | `client_billing_requests` | `apps/api/src/billing`, `apps/api/src/platform-admin` | `kind`, `status`, linkage to organization and optional linked billing request | Enum-backed `kind` and `status` verified in schema. Workflow invariants between submitted, under_review, approved, declined, cancelled must be verified in service/tests. | `client-billing-request.service.ts`, platform-admin client request actions | No idempotency rule verified yet | `/billing/requests*`, platform-admin client request inbox | Deep | Bridges tenant-facing request capture and admin billing execution. |
| Imports | `import_batches` | `apps/api/src/imports` | batch tracking fields in schema | Exact uniqueness and rollback rules not verified yet | `imports.service.ts`, `migration.service.ts`, controller batch routes | Rollback and commit safety rules must be verified in service/tests | `/imports/*` | Standard | Import commit can fan into customer and appointment writes. |
| Onboarding | `onboarding_progress` | `apps/api/src/onboarding` | progress fields in schema | Exact step gating rules verified mainly in web tests; DB invariants need source verification | `onboarding.service.ts` | No special transaction rule verified | `/onboarding/progress` | Standard | Onboarding also coordinates workspace and first-run flows. |
| Help / AI | `assistant_thread_memories`, `ai_credits`, `ai_usage_events`, `ai_budgets` | `apps/api/src/help`, `apps/api/src/ai` | budget, usage, and memory tables | Exact retention and quota rules must be verified in AI and help services | `assistant-thread-memory.service.ts`, `ai-usage.service.ts`, AI execution services | Concurrency and idempotency services exist in `apps/api/src/ai`; verify exact contract before change | `/help/assistant/*`, `/ai/*` | Deep | AI policy and quota drift can affect cost and safety. |
| Privacy / Audit | `consent_audit_logs`, `audit_logs`, `privacy_requests` | `apps/api/src/security`, `apps/api/src/help`, `apps/api/src/platform-admin` | audit and privacy request tracking fields | Exact retention and redaction invariants not verified in bootstrap | privacy controller and audit log services | `TODO: Fill after repository analysis. Do not treat as verified.` | `/privacy/*`, audit consumers | Deep | Privacy exports and anonymize flow are irreversible. |

