# API Contracts

Purpose:

Map important frontend-backend contracts.

This file is map only.

It is not proof of behavior.

Verify all conclusions against real source code, tests, types, schemas, routes, controllers, services, stores, components, API contracts, and database definitions.

If this map conflicts with source code, source code wins.

Mark stale or conflicting entries as `CONTRACT DRIFT`.

Mark missing contracts as `UNMAPPED CONTRACT`.

## Contract Index

| Domain | Feature | Method | Endpoint / Route | Frontend Caller | Backend Handler | Request Shape | Response Shape | Auth / Permission | Risk | Notes |
|---|---|---|---|---|---|---|---|---|---|---|
| Billing Requests | Billing status | `GET` | `/billing/status` | `apps/web/src/hooks/use-billing-status.ts`, `apps/web/src/app/(dashboard)/settings/billing/page.tsx`, `apps/web/src/app/(dashboard)/settings/page.tsx` | `apps/api/src/billing/billing.controller.ts` | none verified beyond authenticated request | `BillingStatusResponse` used in web. Verify exact DTO in source. | Dashboard-authenticated caller. Exact guard: `TODO: Fill after repository analysis. Do not treat as verified.` | Deep | Core billing banner and settings load depend on this contract. |
| Billing Requests | Plan catalog | `GET` | `/billing/plans` | `apps/web/src/app/pricing/page.tsx`, `apps/web/src/app/(dashboard)/settings/billing/page.tsx` | `apps/api/src/billing/billing.controller.ts` | none | Array of plan records. Exact fields: `TODO: Fill after repository analysis. Do not treat as verified.` | Public and dashboard callers both exist. Verify source guards. | Deep | Shared route serves pricing and settings flows. |
| Plan Upgrades | Direct billing mutation | `POST` | `/billing/checkout`, `/billing/addons/checkout`, `/billing/change-plan`, `/billing/cancel`, `/billing/resume`, `/billing/customer-portal` | `apps/web/src/app/(dashboard)/settings/billing/page.tsx` | `apps/api/src/billing/billing.controller.ts` | Request bodies exist in page code. Exact fields: `TODO: Fill after repository analysis. Do not treat as verified.` | Checkout URL or completion signal. Verify each route DTO. | Owner or billing-enabled dashboard caller. Exact policy: verify in source. | Deep | Compatibility risk high because checkout, cancel, and resume all mutate subscription state. |
| Billing Requests | Client billing request lane | `GET`, `POST` | `/billing/requests`, `/billing/requests/:id/cancel` | `apps/web/src/components/billing/client-billing-requests.api.ts`, billing page tests | `apps/api/src/billing/client-billing-request.controller.ts` | `ClientBillingRequest` submit and cancel payloads exist in UI API helper. Exact shape: `TODO: Fill after repository analysis. Do not treat as verified.` | `ClientBillingRequest` or collection shape. Verify source DTO. | Dashboard-authenticated caller. Exact permission path: verify. | Deep | This path feeds platform-admin client request inbox. |
| Platform Admin | Client request inbox | `GET`, `POST` | `/platform-admin/client-billing-requests`, `/platform-admin/client-billing-requests/:id`, `/start-review`, `/approve`, `/decline` | `apps/web/src/components/platform-admin/client-requests/platform-admin-client-requests.api.ts` | `apps/api/src/platform-admin/platform-admin.controller.ts` | Query and action bodies exist in UI helper. Exact DTOs: `TODO: Fill after repository analysis. Do not treat as verified.` | Inbox list and detail shapes. Verify source. | `platform-admin` session required. Exact guard path in `platform-admin.guard.ts`. | Deep | Approve path can create linked manual billing request. |
| Platform Admin | Manual billing inbox | `GET`, `POST` | `/platform-admin/billing-requests`, `/platform-admin/billing-requests/:id`, `/payments`, `/void`, `/send-payment-request-email`, `/send-payment-acknowledgment-email`, `/manual-payments/:paymentId/*` | `apps/web/src/components/platform-admin/billing/platform-admin-billing.api.ts` | `apps/api/src/platform-admin/platform-admin.controller.ts` | Exact request bodies: `TODO: Fill after repository analysis. Do not treat as verified.` | List/detail/payment action shapes: `TODO: Fill after repository analysis. Do not treat as verified.` | `platform-admin` guard | Deep | Payment confirmation and void actions touch billing truth. |
| Automations | Automation settings | `GET`, `PATCH` | `/automation/settings`, `/automation/refine-message`, `/automation/previews` | `apps/web/src/app/(dashboard)/settings/page.tsx` | `apps/api/src/automation/automation.controller.ts` | `businessId` query and settings payload verified in caller paths. Exact fields: `TODO: Fill after repository analysis. Do not treat as verified.` | Settings snapshot and preview payloads. Verify source. | Dashboard-authenticated business-scoped caller | Deep | Automation settings and message refinement both feed scheduled sends. |
| Customers | Customer templates and defaults | `GET`, `POST`, `PATCH`, `DELETE` | `/customers/templates`, `/customers/templates/:templateId`, `/customers/default-template` | `apps/web/src/app/(dashboard)/settings/page.tsx`, `apps/web/src/components/customers/customer-form-modal.tsx` | `apps/api/src/customers/customers.controller.ts` | Query params `businessId`, `businessType` verified in callers. Bodies: verify DTOs. | Template records and default template payloads. Verify source. | Dashboard-authenticated business-scoped caller | Standard | Settings page and customer form share this contract. |
| Customers | Booking-side customer resolution | `POST` | `/customers/resolve-for-booking` | `apps/web/src/app/(dashboard)/appointments/page.tsx` | `apps/api/src/customers/customers.controller.ts` | Body exists in appointments page. Exact fields: `TODO: Fill after repository analysis. Do not treat as verified.` | Resolved customer or candidate payload. Verify source. | Dashboard-authenticated caller | Standard | Crosses appointment create flow and customer matching logic. |
| Customers | Customer activity and messaging | `PATCH`, `GET`, `POST` | `/customers/:id/visit`, `/customers/:id/message-history`, `/customers/:id/visit-adjustment-history`, `/customers/:id/send-follow-up` | `apps/web/src/components/customers/customer-visit-adjustment-modal.tsx`, `customer-message-history-modal.tsx`, `customer-visit-history-modal.tsx` | `apps/api/src/customers/customers.controller.ts` | Exact bodies: verify source | History lists and follow-up action signals. Verify source. | Dashboard-authenticated caller | Standard | Follow-up route likely crosses messaging contracts. |
| Appointments | Appointment board and booking assist | `GET`, `PATCH` | `/appointments/needs-review`, `/appointments/booking/availability`, `/appointments/:id`, `/appointments/:id/status`, `/appointments/:id/arrive`, `/appointments/:id/reminder-sent` | `apps/web/src/app/(dashboard)/appointments/page.tsx` | `apps/api/src/appointments/appointments.controller.ts` | Query/body shapes exist in page tests. Exact DTOs: `TODO: Fill after repository analysis. Do not treat as verified.` | Appointment lists and mutation results. Verify source. | Dashboard-authenticated business-scoped caller | Standard | Booking availability and status mutations share same controller. |
| Appointments | Share templates | `GET`, `POST`, `PATCH`, `DELETE` | `/appointments/share-templates`, `/appointments/share-templates/:id` | `apps/web/src/components/share-slots/share-slots-workflow.tsx` | `apps/api/src/appointments/appointments.controller.ts` | Template create/update bodies exist in caller. Exact fields: verify source. | Share template records. Verify source. | Dashboard-authenticated business-scoped caller | Standard | Feeds public intake link creation. |
| Intake / Booking | Public intake flow | `GET`, `POST` | `/intake/config`, `/intake/availability`, `/intake`, `/intake/hold`, `/intake/otp/send`, `/intake/otp/verify` | `apps/web/src/app/intake/[businessId]/page.tsx` | `apps/api/src/intake/intake.controller.ts` | Query params `businessId`, `month` verified in page. Bodies exist in fetch calls. Exact DTOs: verify source. | Public booking config, availability, OTP, and create responses. Verify source. | Public caller with OTP or booking security flow | Standard | Public contract. Treat auth and abuse controls as sensitive. |
| Platform Admin | Ops and communications | `GET`, `PATCH` | `/platform-admin/communications*`, `/platform-admin/automation-runs*`, `/platform-admin/provider-health`, `/platform-admin/alerts*`, `/platform-admin/audit-logs`, `/platform-admin/session`, `/platform-admin/organizations*` | `apps/web/src/components/platform-admin/{communications,operations,overview,businesses,audit}/*.api.ts`, `platform-admin.api.ts` | `apps/api/src/platform-admin/platform-admin.controller.ts` | Query filters and patch payloads exist in UI helpers. Exact shapes: verify source. | Session, list, alert, and detail payloads. Verify source. | `platform-admin` guard | Deep | These views read and sometimes mutate provider and alert state. |
| Help / AI | Assistant chat and action confirm | `POST` | `/help/assistant/chat`, `/help/assistant/chat/stream`, `/help/assistant/actions/confirm` | `apps/web/src/components/tyvera-assistant.tsx` | `apps/api/src/help/assistant.controller.ts` | Chat prompt, streamed prompt, and action confirm bodies exist in caller. Exact JSON: verify source. | Chat response, stream events, action ack. Verify source. | Dashboard-authenticated caller. Tool policy in help module also applies. | Deep | AI and mutation policy boundary. Do not infer tool contract details. |
| Help / AI | Read-only answer sources | `GET` | `/help/answer-source/business-summary`, `/sms-usage`, `/billing-status`, `/ai-usage` | Direct frontend caller not verified from quick scan | `apps/api/src/help/answer-source.controller.ts` | Query shapes: `TODO: Fill after repository analysis. Do not treat as verified.` | Summary read models. Verify source. | Authenticated business-scoped caller likely. Verify exact guard. | Standard | Useful for planner discovery. Caller mapping incomplete. |
| Imports | Import pipeline | `POST`, `GET` | `/imports/fetch-provider`, `/parse`, `/parse-ocr`, `/duplicates`, `/dry-run`, `/commit`, `/validate`, `/mapping-suggest`, `/mapping-preview`, `/batches*`, `/batches/:batchId/rollback` | Imports route exists at `apps/web/src/app/(dashboard)/imports`. Direct caller files not fully inspected. | `apps/api/src/imports/imports.controller.ts` | `TODO: Fill after repository analysis. Do not treat as verified.` | `TODO: Fill after repository analysis. Do not treat as verified.` | Dashboard-authenticated caller likely. Verify exact guard. | Standard | Contract map intentionally partial until UI caller readback is done. |

