# Module Ownership Map

Purpose:

Map product and business domains to implementation areas.

This file is map only.

It is not proof of behavior.

Verify all conclusions against real source code, tests, types, schemas, routes, controllers, services, stores, components, API contracts, and database definitions.

If this map conflicts with source code, source code wins.

Mark stale or conflicting entries as `CONTEXT DRIFT`.

Mark missing domains as `UNMAPPED DOMAIN`.

## Domain Index

| Domain | Frontend | Backend | Database / Schema | Jobs / Automations | Integrations | Tests | Risk | Notes |
|---|---|---|---|---|---|---|---|---|
| Billing Requests | `apps/web/src/app/(dashboard)/settings/billing`, `apps/web/src/components/billing`, `apps/web/src/app/(platform-admin)/platform-admin/{client-requests,billing-requests}` | `apps/api/src/billing/client-billing-request.controller.ts`, `apps/api/src/billing/*.service.ts`, `apps/api/src/platform-admin/*billing*` | `clientBillingRequests`, `manualBillingRequests`, `manualBillingRequestItems`, `manualPayments`, `manualBillingFulfillments`, `manualBillingEmailDeliveries`, `organizations` in `packages/database/src/schema/index.ts` | No dedicated async job verified. Review platform-admin flows and billing webhook handlers. | PayMongo, LemonSqueezy, email delivery hooks | `apps/api/src/billing/*.spec.ts`, `apps/api/src/platform-admin/*.spec.ts`, `apps/web/src/app/(dashboard)/settings/billing/*.test.tsx` | Deep | Client request inbox and manual billing flow both present. Verify exact guard and DTO before planning. |
| Payments | `apps/web/src/app/(dashboard)/settings/billing`, `apps/web/src/components/platform-admin/billing` | `apps/api/src/billing/billing.controller.ts`, `apps/api/src/billing/billing-webhook.controller.ts`, `apps/api/src/platform-admin/platform-admin-billing.service.ts` | `manualPayments`, `manualBillingRequests`, `manualBillingFulfillments`, `subscriptions`, `organizations` | Billing webhooks and payment confirmation actions | PayMongo, LemonSqueezy, email | Billing and platform-admin specs above | Deep | Webhook path and manual payment confirmation both exist. Treat provider contracts as high risk. |
| SMS Credits | `apps/web/src/app/(dashboard)/settings/billing`, `apps/web/src/components/platform-admin/businesses` | `apps/api/src/messaging/*`, `apps/api/src/billing/sms-addon-grant.service.ts`, `apps/api/src/platform-admin/platform-admin.controller.ts` | `smsCredits`, `smsUsageEvents`, `smsAddons`, `creditReconciliationEvents`, `clientBillingRequests` | Credit reconciliation service paths exist in billing and messaging code; review related jobs before mutation | Semaphore, Twilio, billing providers | `apps/api/src/messaging/*.spec.ts`, `apps/api/src/billing/sms-addon-grant.service.spec.ts`, `apps/api/src/billing/credit-reconciliation.spec.ts` | Deep | Admin SMS adjustments and provider metering both touch credit truth. |
| Plan Upgrades | `apps/web/src/app/(dashboard)/settings/billing`, `apps/web/src/components/billing/request-change-section.tsx` | `apps/api/src/billing/billing.controller.ts`, `apps/api/src/billing/client-billing-request.service.ts`, `apps/api/src/platform-admin/platform-admin-billing.service.ts` | `organizations`, `subscriptions`, `clientBillingRequests`, `manualBillingRequests`, `verifiedOnlineBookingAddons`, `smsAddons` | No standalone scheduler verified. Review billing webhook and admin fulfillment flow. | PayMongo, LemonSqueezy | Billing page tests and billing specs | Deep | Direct checkout and request-based plan change paths both exist. |
| Auth / Permissions | `apps/web/src/app/sign-{in,up}`, `apps/web/src/lib/{auth-client,protected-routes}.ts`, platform-admin shell guards | `apps/api/src/auth/*`, `apps/api/src/common/*guard*.ts`, `apps/api/src/platform-admin/platform-admin.guard.ts`, `apps/api/src/security/*` | `users`, `authIdentities`, `authOtpChallenges`, `authSessions`, `platformAdmins`, `adminRoles`, `adminPermissions`, `adminRolePermissions`, `platformAdminRoles` | Session bootstrap and OTP challenge flows exist; no external scheduler verified | Clerk | `apps/api/src/auth/*.spec.ts`, `apps/api/src/common/*guard*.spec.ts`, `apps/api/src/platform-admin/platform-admin.guard.spec.ts`, auth client tests | Deep | Role and workspace selection affect most routes. |
| Customers | `apps/web/src/app/(dashboard)/customers`, `apps/web/src/components/customers`, `apps/web/src/app/(dashboard)/settings/page.tsx` | `apps/api/src/customers/*`, `apps/api/src/crm/*`, `apps/api/src/security/privacy.controller.ts` | `customers`, `customerDescriptionTemplates`, `businessDefaultDescriptionTemplates`, `visitAdjustmentHistory`, CRM tables in schema | Visit follow-up and message history interact with messaging. No background job verified yet. | Messaging providers through customer follow-up paths | `apps/api/src/customers/*.spec.ts`, customer UI tests under `apps/web/src/components/customers` and dashboard tests | Standard | Customer templates and default template settings verified in web and API. |
| Appointments | `apps/web/src/app/(dashboard)/appointments`, `apps/web/src/components/share-slots` | `apps/api/src/appointments/*`, `apps/api/src/help/*appointment*`, `apps/api/src/workflows/*` | `appointments`, `appointmentShareTemplates`, `bookingSecuritySettings`, `bookingPinAttemptLogs`, `bookingHolds` | Appointment lifecycle scheduler and reminder flags exist in `apps/api/src/appointments` and automation code | Messaging providers when reminders fire | `apps/api/src/appointments/*.spec.ts`, `apps/web/src/app/(dashboard)/appointments/*.test.tsx` | Standard | Booking security and reminder state raise risk if task crosses public booking flow. |
| Intake / Booking | `apps/web/src/app/intake/[businessId]`, `apps/web/src/components/intake-qr-block.tsx`, share-slots workflow | `apps/api/src/intake/*`, booking endpoints in `apps/api/src/appointments/appointments.controller.ts` | `bookingHolds`, `bookingSecuritySettings`, `bookingPinAttemptLogs`, `publicOtpSendEvents`, `appointments`, `customers` | OTP send and verification flow exists; appointment booking hold flow present | Twilio or Semaphore for OTP and messaging | `apps/api/src/intake/*.spec.ts`, `apps/web/src/app/intake/[businessId]/*.test.tsx`, appointment wizard tests | Standard | Public flow. Verify auth-free routes and OTP invariants before change. |
| Automations | `apps/web/src/app/(dashboard)/settings/page.tsx`, `apps/web/src/app/(platform-admin)/platform-admin/automation-runs` | `apps/api/src/automation/*`, `apps/api/src/operations/automation-job-run.service.ts` | `automationSettings`, `automationJobRuns`, related message event tables | Scheduler, send, trigger, composer, and operations alert paths exist | Messaging providers, OpenAI for refinement | `apps/api/src/automation/*.spec.ts`, `apps/api/src/operations/automation-job-run.service.spec.ts`, settings page tests | Deep | Enabled templates and send jobs affect outbound messaging at scale. |
| Messaging | `apps/web/src/components/tyvera-assistant.tsx`, dashboard flows that trigger follow-up and share links, platform-admin communications pages | `apps/api/src/messaging/*`, `apps/api/src/help/answer-source.controller.ts`, `apps/api/src/platform-admin/platform-admin-communications.service.ts` | `messageEvents`, `manualFollowUpTasks`, `smsCredits`, `smsUsageEvents`, `emailCredits`, `emailUsageEvents`, `processedWebhookEvents`, `creditReconciliationEvents` | Manual follow-up retry, webhook reconciliation, provider health, automation sends | Twilio, Semaphore, Resend, OpenAI in generation flow | `apps/api/src/messaging/*.spec.ts`, platform-admin communications specs | Deep | Includes messaging webhooks. Verify provider-specific paths in source. |
| Imports | `apps/web/src/app/(dashboard)/imports` | `apps/api/src/imports/*` | `importBatches`, likely customer tables on commit | Import commit and rollback endpoints exist | CSV/XLSX/OCR providers in `apps/api/src/imports/providers` | `apps/api/src/imports/*.spec.ts` | Standard | Caller files for each endpoint not fully inspected. Verify import UI call sites before contract change. |
| Insights / Analytics | `apps/web/src/app/(dashboard)/analytics`, `apps/web/src/app/(dashboard)/insights`, `apps/web/src/components/insights` | `apps/api/src/insights/*`, `apps/api/src/admin/admin.controller.ts`, `apps/api/src/help/answer-source.controller.ts` | No dedicated insights table verified from quick scan. Likely derived from appointments, customers, messaging, billing tables. | No standalone job verified | None verified beyond core DB reads | `apps/api/src/insights/*.spec.ts` | Standard | Treat metrics as read models until query ownership verified. |
| Platform Admin | `apps/web/src/app/(platform-admin)/platform-admin/*`, `apps/web/src/components/platform-admin/*` | `apps/api/src/platform-admin/*`, `apps/api/src/admin/*`, `apps/api/src/operations/*`, `apps/api/src/security/audit-log.service.ts` | `platformAdmins`, admin role tables, `platformAdminAuditLogs`, billing tables, `operationsAlerts`, `providerHealthSnapshots`, `automationJobRuns` | Alerts, provider health, billing review, audit logs, automation run inspection | PayMongo, LemonSqueezy, Twilio, Semaphore, Resend | `apps/api/src/platform-admin/*.spec.ts`, `apps/api/src/admin/*.spec.ts`, platform-admin UI tests | Deep | Admin surfaces can mutate billing, credits, and permissions. |
| Settings / Onboarding | `apps/web/src/app/(dashboard)/settings`, `apps/web/src/app/onboarding`, `apps/web/src/components/onboarding`, `apps/web/src/lib/onboarding.ts` | `apps/api/src/onboarding/*`, `apps/api/src/organizations/*`, `apps/api/src/businesses/*`, `apps/api/src/users/*` | `onboardingProgress`, `organizations`, `businesses`, `users`, `customerDescriptionTemplates` | Onboarding flow config interacts with workspace selection and initial templates | Clerk for auth bootstrap | `apps/web/src/lib/onboarding*.test.ts`, `apps/api/src/onboarding/*.spec.ts`, business and org controller tests where present | Standard | Settings page also touches automation and billing. Verify boundary before multi-domain changes. |

