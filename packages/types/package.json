{
  "name": "@tyvera/types",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test:run": "vitest run",
    "lint": "eslint . --ext .ts",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@tyvera/config": "workspace:*",
    "typescript": "^5.6.2",
    "vitest": "^3"
  }
}
